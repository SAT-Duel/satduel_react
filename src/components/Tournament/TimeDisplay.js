import styled from 'styled-components';

const TimeDisplay = styled.div`
    font-size: 1.5rem;
    font-weight: bold;
    color: #1a1a1a;
    text-align: center;
    margin-top: 16px;
`;

export default TimeDisplay;
